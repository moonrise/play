create CIS internal session via web service call.

package the distributable zip file by executing npm from the directory where package.json is located:

1. make sure the java program is built (out directory is created with appropriate class files)
2. 'npm install' if node_modules are not populated
3. 'npm run zipit' and find cisssltest.zip in 'out' directory
